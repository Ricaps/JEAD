# Absolute path to the models directory on your host machine
MODELS_ROOT_HOST=/home/user/models_root

# Set to true to enable NVIDIA GPU acceleration in the inference server container.
# Requires NVIDIA drivers and the NVIDIA Container Toolkit on the host.
# When set to true, start the server with: docker compose -f compose.yaml -f compose.gpu.yaml up -d
USE_GPU=false
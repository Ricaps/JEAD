services:
  inference-server-cpu:
    image: docker.io/ricaps/jead:latest
    pull_policy: always
    ports:
      - "8090:8080"
    environment:
      ADDRESS: 0.0.0.0
      PORT: 8080
      MODELS_ROOT: /usr/models
      PIP_CACHE_DIR: /root/.cache/pip
    volumes:
      - type: bind
        source: ${MODELS_ROOT_HOST}
        target: ${MODELS_ROOT}
      - pip-cache:/root/.cache/pip

volumes:
  pip-cache: